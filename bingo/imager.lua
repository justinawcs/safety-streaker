local imager = {}

function imager.test()
    return ("Hello World!!")
end

function imager.pic(image, scale, startX, startY)
    iwidth, iheight = image:size()
    owidth = iwidth
end

return imager
